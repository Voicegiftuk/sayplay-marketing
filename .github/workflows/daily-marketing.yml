name: SayPlay Daily Marketing

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * *'

jobs:
  generate-content:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install
        run: pip install google-generativeai
      
      - name: Generate
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python sayplay_marketing_system.py
      
      - name: Commit
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Daily content'
```

4. **Click:** "Commit changes"

5. **Wait 10 seconds**

6. **Go back to Actions tab**

7. **You should NOW see "SayPlay Daily Marketing" on the left!**

---

## 🎯 DO THIS NOW:

**Click this link to edit the file:**

👉 https://github.com/Voicegiftuk/sayplay-marketing/edit/main/.github/workflows/daily-marketing.yml

**Replace with the code above, then tell me:**
```
✅ Replaced the file!
