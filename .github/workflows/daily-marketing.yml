name: SayPlay Daily Marketing

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  generate-content:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install google-generativeai

      - name: Generate content
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python sayplay_marketing_system.py

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Daily content'
          commit_user_name: 'SayPlay Bot'
          commit_user_email: 'bot@voicegift.uk'
```

4. **Commit changes**

---

## 🎯 WHY THIS WORKS:

✅ Uses `git-auto-commit-action` (tested by millions)  
✅ No manual bash commands  
✅ No YAML syntax issues  
✅ Handles everything automatically  

---

## 🚀 RUN IT:

**Actions → Run workflow**

**This WILL work!** 💯

Reply:
```
✅ Testing now!
